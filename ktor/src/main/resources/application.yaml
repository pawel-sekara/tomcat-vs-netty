ktor:
    application:
        modules:
            - dev.sekara.block.ktor.ApplicationKt.module
    deployment:
        port: 8081

db:
  uri: "r2dbc:pool:postgresql://db:5432/ktor?initialSize=20&maxSize=100"
  user: "root"
  password: "root"